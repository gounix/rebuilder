apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rebuilder.fullname" . }}-test-connection"
  labels:
    {{- include "rebuilder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rebuilder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
