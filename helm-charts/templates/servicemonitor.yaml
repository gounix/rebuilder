{{- if and .Values.metrics.enabled .Values.metrics.serviceMonitor.enabled -}}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ template "rebuilder.fullname" . }}-servicemonitor
  namespace: {{ .Values.metrics.serviceMonitor.namespace }}
  labels:
    app: {{ template "rebuilder.name" . }}-metrics
{{- if .Values.metrics.serviceMonitor.labels }}
{{ toYaml .Values.metrics.serviceMonitor.labels | indent 4 }}
{{- end }}
spec:
  namespaceSelector:
    matchNames:
      - {{ template "rebuilder.fullname" . }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ template "rebuilder.name" . }}
  endpoints:
  - port: metrics
    scheme: http
    path: /metrics
{{- end }}

